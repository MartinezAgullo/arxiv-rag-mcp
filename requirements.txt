# MCP SDK - Use a middle-ground version
# 1.0.0 is too old (doesn't speak protocol these servers use)
# 1.20.0 is too new (protocol incompatible)
# Try versions in between!
mcp==1.1.0

# OpenAI for GPT-4
openai>=1.0.0

# HTTP clients
httpx>=0.27.0
aiohttp>=3.9.0

# Configuration
python-dotenv>=1.0.0
pydantic>=2.0.0

# Utilities
tenacity>=8.2.0
structlog>=23.0.0
anyio>=4.0.0